import React, { Component } from 'react'

export class AddQuestion extends Component {
    render() {
        return (
            <button className="btn btn-dark">Add question</button>
        )
    }
}

export default AddQuestion
